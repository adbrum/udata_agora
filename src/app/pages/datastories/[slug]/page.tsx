import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Data Story - dados.gov',
};

export default function DataStoryDetailPage() {
    return (
        <div className="flex-grow bg-white min-h-[60vh]">
            {/* Empty content as requested */}
        </div>
    );
}
